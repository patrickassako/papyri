// Use require() for better compatibility with React Native
const colors = require('./colors.json');
const typography = require('./typography.json');
const spacing = require('./spacing.json');
const shapes = require('./shapes.json');

module.exports = { colors, typography, spacing, shapes };
